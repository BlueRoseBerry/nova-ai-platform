spring:
  application:
    name: nova-ai-agent
  threads:
    virtual:
      enabled: true
  datasource:
    url: ${DB_URL:jdbc:postgresql://10.253.170.88:35432/postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:test@2026}
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8082
